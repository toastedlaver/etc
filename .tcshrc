## -*- coding: iso-2022-7bit -*-
##################################################
##   .cshrc for Cygwin                          ##
##                                              ##
##################################################
#!/usr/local/bin/tcsh

### 言語設定 #########################################################
## locale関係の環境変数: LANG, LC_*, LANGUAGE
#  LANG、LC_ALL で一括設定、LC_MESSAGES や LC_DATE でカテゴリ毎の言語設定が出来るようになっている。
#  LANGUAGE は LC_MESSAGES を上書きするようだ。
#  優先順位は LANGUAGE > LC_ALL > LC_* > LANG
#setenv LANG "ja_JP.eucJP"
setenv LANG "ja_JP.SJIS"

## コマンドメッセージの日本語化
# 環境変数 NLSPATH には、catgets が利用するメッセージカタログの位置を
# 設定します。
setenv NLSPATH /usr/share/locale/%L/%N

### less の設定 ######################################################
setenv PAGER "less"
## デフォルトオプション
#  -r オプションでコントロールコードをそのまま表示 → 日本語表示
#  -x でタブ幅指定
setenv LESS -rx4
## 文字コード設定
#  設定すると「invalid charset name」となるので設定不可。何故だ…orz
#setenv LESSCHARSET japanese-sjis

### PATH #############################################################
#setenv PATH "/usr/local/bin:/usr/X11R6/bin:/usr/bin:/bin:${PATH}"
#setenv PATH "/usr/local/sbin:/usr/sbin:/sbin:${PATH}"

### その他 ###########################################################
## タイムゾーンの設定
setenv TZ "JST-09"

## CVS
# コマンド書式: %cvs -d :ext:username@hostname:/usr/local/cvsroot
#setenv CVSROOT ":ext:192.168.97.1:/mnt/ext1/cvsroot"
#setenv CVS_RSH "ssh"

## make 実行時に /bin/sh を使う (dos にすると command.com を使う)
setenv MAKE_MODE "unix"

## 一時ディレクトリを指定
# TEMP は今日びの cygwin なら指定せんでも動く。逆にコレのせいで emacs が落ちなくなった。。。
#setenv TEMP "~/AppData/Local/Temp"

### シェル設定 #######################################################
## tcsh で日本語を利用可能にするには、dspmbyte に利用する文字コードを
# 設定します。
#set dspmbyte=sjis
#set dspmbyte=euc

## 補完時に候補を複数表示する
set autolist

## 時間経過で自動ログアウトしない
unset autologout

## プロンプトに何も入力していない状態で Ctrl+D 押してもログアウトしない
# ※入力された状態ならば、 Ctrl+D で補完できる
set ignoreeof

## コマンドを履歴へ保存する際の挙動を「重複するコマンドを削除」にする
set histdup = erase

## Ctrl+S での stop をやめる
#stty stop undef

### Alias ########################################
## [メモ]コマンドの前に '\' をつけることで、一時的にエイリアスを無視できる
alias ls "ls -F --color=auto --show-control-chars"
alias la "ls -a"
alias ll "ls -l"
alias l "ls -la"
alias rm "rm -i"
alias cp "cp -i"
alias mv "mv -i"
alias mea "meadow &"
alias em "winemacs &"
## Cygwin の diff は改行コード LF なので、 CRLF の diff 作成すると差分の行だけ改行コードが CRLF になる
## そのままだと patch が当てられないので、 --binary オプションをつけて回避する。
## ※パッチファイルの改行コードを nkf などで変えると、その patch を当てたファイルの改行コードが全て LF に変化してしまう
alias patch "patch --binary"

### Prompt #######################################
set prompt="%{\e[32m%}[%n@%m %{\e[31m%}%B%c3%b%{\e[32m%}]%{\e[00m%}% "

## エスケープシーケンスの解説
#\a ASCIIのベル文字（07）を表示する（ビープ音を鳴らす）
#\d 「曜 日 月 日」の形式（例：Fri Jan 5）で日付を表示する
#\e ASCIIのエスケープ文字（033）を表示する
#\h ホスト名のうち最初の「.」までの部分を表示する
#\H ホスト名を表示する
#\n 改行する
#\r 復帰する
#\s シェルの名前を表示する
#\t 現在の時刻を24時間の「HH:MM:SS」形式で表示する
#\T 現在の時刻を12時間の「HH:MM:SS」形式で表示する
#\@ 現在の時刻を12時間の「am/pm」形式で表示する
#\u 現在のユーザー名を表示する
#\v bashのバージョンを表示する
#\V bashのリリースを表示する
#\w  現在の作業ディレクトリを、ユーザーのホームディレクトリからの絶対パスで表示する
#\W 現在の作業ディレクトリを表示する
#\! このコマンドの履歴番号を表示する
#\# このコマンドのコマンド番号（現在のシェルのセッション中に実行されたコマンドのシーケンスにおける位置）を表示する
#\$ 実効UIDが0の場合に#となり、それ以外の場合に$となる
#\nnn 8進数nnnに対応する文字を表示する
#\\ バックスラッシュを表示する
#\[ 非表示文字のシーケンスを開始する。これを使って、プロンプト中に端末の制御シーケンスを埋め込むことができる
#\] 非表示文字のシーケンスを終了する
#
# \e[xx<;xx<;xx>...>m
#    00 … (初期状態へ、理解しない端末もある)
#    01 … bold(太字)
#    04 … 文字を blue (アンダーライン)
#    05 … ？ (blink(太字になる端末が多い))
#    07 … reverse(反転)
#    30 … 文字を black
#    31 … 文字を red
#    32 … 文字を green
#    33 … 文字を yellow
#    34 … 文字を blue
#    35 … 文字を magenta
#    36 … 文字を cyan
#    37 … 文字を white
#    40 … 背景を black
#    41 … 背景を red
#    42 … 背景を green
#    43 … 背景を yellow
#    44 … 背景を blue
#    45 … 背景を magenta
#    46 … 背景を cyan
#    47 … 背景を white

### [EOF] ###
